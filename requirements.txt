numpy==1.26.4
requests
opencv-python-headless
insightface
onnxruntime-gpu==1.16.3
runpod
paddleocr==2.10.0
# paddlepaddle-gpu is installed separately in Dockerfile with the CUDA 11.8
# build (post117 = CUDA 11.7, backward-compatible with CUDA 11.8).
# The runtime image only ships libcudnn.so.8 — the Dockerfile adds a libcudnn.so
# symlink so PaddlePaddle's dlopen("libcudnn.so") succeeds at runtime.
